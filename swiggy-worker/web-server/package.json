{
  "name": "web-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "prisma generate && tsc -b",
    "start": "prisma generate && tsc -b && node dist/index.js",
    "seeder": "tsc -b && node dist/jobs/seederJob.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "^6.9.0",
    "@types/express": "^5.0.3",
    "@types/node-cron": "^3.0.11",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "node-cron": "^4.1.0",
    "zod": "^3.25.63"
  },
  "devDependencies": {
    "prisma": "^6.9.0"
  }
}
